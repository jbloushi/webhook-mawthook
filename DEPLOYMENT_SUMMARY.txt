MAWTHOOK DEPLOYMENT SUMMARY (UPDATED)
====================================

Status: Ready for native VPS deployment (no Docker required)

Primary guide:
- DEPLOY.md (Node.js + PostgreSQL + systemd + aaPanel reverse proxy)

Required working path:
- cd /www/wwwroot/webhook-mawthook

Core setup commands:
- npm ci
- npm run prisma:migrate:deploy
- npm run build
- ADMIN_EMAIL=admin@yourdomain.com ADMIN_PASSWORD='StrongPass!ChangeMe' npm run prisma:seed
- sudo systemctl restart mawthook

Health check:
- GET /api/health

Important:
- URL-encode special characters in DB password inside DATABASE_URL (e.g. @ -> %40).
